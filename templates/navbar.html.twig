{% set currentRoute = app.request.attributes.get('_route') %}

<nav class="navbar navbar-expand-lg bg-primary mb-4" data-bs-theme="dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_site_index') }}">
            <img src="/image/favicon-32x32.png" alt="Logo" width="24" height="24"
                 class="d-inline-block align-text-top me-1">
            vPanel
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item mx-2">
                    <a class="nav-link {{ currentRoute == 'app_site_index' ? 'active' : '' }}"
                       aria-current="page"
                       href="{{ path('app_site_index') }}">
                        <i class="fa-solid fa-link me-1"></i> Sites
                    </a>
                </li>
                <li class="nav-item mx-2">
                    <a class="nav-link {{ currentRoute == 'app_php_version_index' ? 'active' : '' }}"
                       aria-current="page"
                       href="{{ path('app_php_version_index') }}">
                        <i class="fa-solid fa-scroll me-1"></i> PHP Versions
                    </a>
                </li>
                <li class="nav-item mx-2">
                    <a class="nav-link {{ currentRoute == 'app_database_index' ? 'active' : '' }}"
                       aria-current="page"
                       href="{{ path('app_database_index') }}">
                        <i class="fa-solid fa-database me-1"></i> Databases
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>